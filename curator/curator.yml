---
client:
  hosts:
    - ${ELASTICSEARCH_HOST}
  port: ${ELASTICSEARCH_PORT}
  http_auth: ${ELASTICSEARCH_USER}:${ELASTICSEARCH_PASSWORD}
  use_ssl: False
  ssl_no_validate: True
  timeout: 30

logging:
  loglevel: INFO
  logfile:
  logformat: default